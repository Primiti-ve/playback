[package]
name = "playback-logger"
description = "the no-dependency alternative to `log` and `env_logger`"
license = "MIT"
repository = "https://github.com/Primiti-ve/playback"
readme = "README.md"
version = "0.1.0"
edition = "2024"

[dependencies]
